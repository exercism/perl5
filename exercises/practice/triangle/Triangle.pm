package Triangle;

use strict;
use warnings;
use feature qw<say>;

use Exporter qw<import>;
our @EXPORT_OK = qw<is_equilateral is_isosceles is_scalene>;

sub is_equilateral {
    my ($triangle) = @_;
    return undef;
}

sub is_isosceles {
    my ($triangle) = @_;
    return undef;
}

sub is_scalene {
    my ($triangle) = @_;
    return undef;
}

1;
