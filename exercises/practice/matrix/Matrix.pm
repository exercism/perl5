package Matrix;

use strict;
use warnings;
use feature qw<say>;

use Exporter qw<import>;
our @EXPORT_OK = qw<row column>;

sub row {
    my ($input) = @_;
    return undef;
}

sub column {
    my ($input) = @_;
    return undef;
}

1;
