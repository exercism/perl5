package Anagram;

use strict;
use warnings;
use feature qw<say>;

use Exporter qw<import>;
our @EXPORT_OK = qw<match_anagrams>;

sub match_anagrams {
    my ( $subject, $candidates ) = @_;
    return undef;
}

1;
