package ListOps;

use strict;
use warnings;

sub map {
  my ( $func, $list ) = @_;

  return undef;
}

sub reduce {
  my ( $func, $list ) = @_;

  return undef;
}

sub length {
  my $list = shift;

  return undef;
}

1;
