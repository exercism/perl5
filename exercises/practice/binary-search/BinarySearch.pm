package BinarySearch;

use strict;
use warnings;
use feature qw<say>;

use Exporter qw<import>;
our @EXPORT_OK = qw<binary_search>;

sub binary_search {
    my ( $array, $value ) = @_;
    return undef;
}

1;
