package SumOfMultiples;

use strict;
use warnings;
use feature qw<say>;

use Exporter qw<import>;
our @EXPORT_OK = qw<sum_of_multiples>;

sub sum_of_multiples {
    my ( $factors, $limit ) = @_;
    return undef;
}

1;
